# Agreed 기술 설계 문서

---

## 1. 저장소 구성 방침

본 프로젝트는 프론트엔드와 백엔드를 하나의 Next.js 저장소에서 관리하는 단일 저장소 구조를 채택한다.

프레임워크를 분리하면 실행 프로세스가 둘로 늘고, 그에 따라 포트 설정·CORS·환경변수 동기화라는 실패 지점이 함께 늘어난다. 현장 시연을 전제로 하는 개발에서는 이 실패 지점의 수가 곧 위험이다. 반면 프레임워크를 하나 더 도입한다고 해서 설계의 깊이가 증명되지는 않는다.

따라서 기술적 완성도는 프레임워크의 개수가 아니라 **저장소 내부의 계층 분리**로 확보한다. 아래 3절의 계층 규칙이 그 수단이다.

---

## 2. 계층 구조

세 계층으로 구성하며 의존 방향은 한 방향으로만 흐른다.

```
app/api/   ─────▶   core/   ◀─────   infra/
(진입점)          (도메인)          (외부 의존)
```

| 계층 | 역할 |
|---|---|
| `app/api/` | 요청 파싱, 응답 반환, 계층 간 연결 |
| `core/` | 도메인 규칙, 상태 전이, 검증 |
| `infra/` | AI 호출, 저장소 접근, 외부 연동 |

핵심 제약은 하나다. **`core/`는 프레임워크를 참조하지 않는다.** 이 계층에는 `next`, `react`, `fetch`, `process.env`가 등장하지 않으며, 입력을 받아 출력을 내는 순수 함수와 타입만 존재한다.

외부와 통신하는 코드는 전부 `infra/`에 격리하고, `app/api/`가 두 계층을 이어 붙인다.

---

## 3. 디렉터리 구성

```
app/
├─ page.tsx                화면
├─ layout.tsx              레이아웃
├─ globals.css             디자인 토큰
└─ api/                    API 진입점

core/
├─ contract/               계약 도메인
├─ requirement/            요구사항 도메인
│  ├─ state-machine.ts     상태 전이 규칙
│  └─ grounding.ts         근거 검증
└─ shared/

infra/
├─ llm/                    AI 호출, 프롬프트, 폴백
└─ storage/                저장소 어댑터

components/                UI 컴포넌트
types/index.ts             공유 타입
lib/                       공용 유틸
```

---

## 4. 실행 환경

| 항목 | 선택 |
|---|---|
| 프레임워크 | Next.js (App Router) |
| 언어 | TypeScript |
| 스타일 | Tailwind CSS |
| 스키마 검증 | Zod |
| AI | Anthropic API |
| 저장소 | localStorage |
| 배포 | Vercel |

데이터베이스를 도입하지 않는다. 현 단계의 검증 목적은 사용자 경험의 성립 여부이며, 영속성은 검증 대상이 아니다. 상태는 브라우저에 저장하되, 저장소 접근은 `infra/storage`의 어댑터를 통해서만 이루어지도록 격리한다.

---

## 5. 확장 및 이전 계획

현재의 단일 저장소 구조는 개발 기간의 제약에 따른 임시 조치가 아니라, 이후 단계로의 이전 비용을 최소화하도록 의도적으로 설계된 1단계 구조이다.

이 설계의 전제는 2절에서 제시한 계층 제약, 즉 도메인 계층이 프레임워크와 외부 환경을 참조하지 않는다는 규칙이다. 도메인 규칙이 특정 프레임워크의 API나 저장소 구현에 결합되어 있지 않다면, 이후의 구조 변경은 도메인을 다시 작성하는 일이 아니라 주변 계층을 교체하는 일이 된다.

**2단계에서는 저장소를 교체한다.** 사용자 수가 늘고 데이터 영속성이 요구되는 시점에 `infra/storage`의 어댑터 구현을 관계형 데이터베이스로 대체한다. 저장소 접근이 어댑터 뒤에 격리되어 있으므로 변경 범위는 해당 모듈에 국한되며, 도메인 계층과 화면 계층은 수정 대상이 아니다.

**3단계에서는 서버를 분리한다.** 백그라운드 작업, 다중 사용자 권한, 외부 시스템 연동이 필요해지는 시점에 `app/api/`가 담당하던 진입점 역할을 독립 서버로 이관한다. 이때 이전 대상은 진입점 계층에 한정되며, `core/`는 파일 단위로 그대로 이동한다. 프레임워크에 의존하지 않는 순수 TypeScript로 작성되어 있기 때문이다. 프론트엔드는 호출 대상 주소만 변경하면 된다.

**4단계에서는 입력 경로를 확장한다.** 메신저, 전자우편, 회의 도구와의 연동은 새로운 입력 경로를 추가하는 작업이며, 이는 `infra/` 하위에 어댑터를 추가하는 방식으로 처리한다. 입력의 출처가 무엇이든 도메인 계층이 받는 자료 형태는 동일하게 유지되므로, 연동 대상이 늘어나도 도메인 규칙은 변경되지 않는다.

네 단계를 관통하는 공통점은 **도메인 계층이 모든 단계에서 보존된다는 점**이다. 저장소, 프레임워크, 입력 경로는 모두 교체 가능한 주변부로 취급하며, 변경 비용은 해당 어댑터의 크기에 비례한다. 계층 분리에 드는 초기 비용은 이 지점에서 회수된다.

---

## 6. 인공지능 적용 방식과 근거

### 6.1 적용 범위

본 시스템에서 인공지능은 세 지점에 적용한다. 첫째, 계약 문서를 구조화된 조건으로 변환한다. 둘째, 고객과의 대화에서 새로 발생한 요구사항을 추출하고 그 성격을 분류한다. 셋째, 추출된 요구사항이 기존 계약의 어느 조항에 근거하는지를 대조한다.

반대로 인공지능을 적용하지 않는 영역을 명확히 구분한다. 요구사항의 상태 전이는 규칙으로 확정되어 있으므로 코드가 처리한다. 계약 반영 여부의 판단은 합의 성립이라는 사실 확인에 해당하므로 추론의 대상이 아니다. 금액과 일정의 결정은 사용자의 판단 영역이며, 시스템이 대신하지 않는다.

검색 증강 생성(RAG)과 미세조정 역시 채택하지 않았다. 계약 문서는 분량이 짧아 전체를 문맥에 포함할 수 있으며, 이를 분할하여 일부만 참조할 경우 대조에 필요한 조항이 누락되어 정확도가 오히려 저하된다. 미세조정은 학습 자료가 확보되지 않은 현 단계에서 적용할 근거가 없다.

### 6.2 문제 유형의 규정

본 시스템이 다루는 과제는 대화에서 구조화된 상태를 추출하여 지속적으로 갱신하는 문제, 즉 대화 상태 추적(Dialogue State Tracking)에 해당한다. 이는 자연어 처리 분야에서 오랜 기간 연구되어 온 과제 유형이다.

공개 벤치마크에서 대규모 언어모델에 과제 전체를 위임하는 종단간 방식은 낮은 정확도를 보이며, 추출·분류·검증을 분리하여 각 단계를 개별적으로 처리하는 방식이 유의미하게 높은 성능을 기록한다. 또한 화자가 다수인 대화에서는 단일 화자 대비 성능 저하가 관찰된다. 본 시스템이 다루는 대화는 발주자와 수행자가 함께 참여하는 다자 대화이므로, 과제 난이도는 표준 벤치마크보다 높다고 보아야 한다.

이러한 근거에 따라 본 시스템은 언어모델에 과제 전체를 위임하지 않고, 6.3절의 다단계 검증 구조를 채택하였다.

### 6.3 검증 구조

인공지능의 출력을 신뢰하지 않는다는 전제 아래 다섯 계층의 검증을 둔다.

먼저 대화를 발화 단위로 분할하고 각 발화에 식별자를 부여한다. 이는 이후 단계에서 모델의 출력을 원문에 대응시키기 위한 기준점이 된다.

다음으로 모델의 출력 형식을 도구 호출 방식으로 강제하고, 수신한 결과를 스키마로 재검증한다. 검증에 실패한 경우 오류 내용을 포함하여 1회에 한해 재요청하며, 반복 실패 시에는 폴백 경로로 전환한다.

세 번째 계층에서는 모델이 제시한 근거 인용문이 원문에 실제로 존재하는지를 대조한다. 존재하지 않는 인용은 모델이 생성한 허구로 간주하여 해당 항목을 폐기한다. 이때 전체 결과를 폐기하지 않고 항목 단위로 처리하는 부분 수용 방식을 적용하여, 일부 오류가 전체의 실패로 확대되지 않도록 한다.

네 번째 계층에서는 모델이 제안한 상태가 이전 상태로부터 도달 가능한지를 검증한다. 도달 불가능한 경우 요청을 거부하는 대신 미확정 상태로 강등한다. 거부는 화면에 아무 정보도 남기지 않지만, 강등은 사용자가 판단할 대상을 남기기 때문이다.

마지막 계층에서는 사용자 승인을 확인한다. 합의가 성립하지 않은 요구사항은 계약에 반영될 수 없으며, 이 검사를 우회하는 경로는 시스템 내에 존재하지 않는다.

### 6.4 설계상의 특징

본 설계의 특징은 모델의 불확실성을 은폐하지 않고 도메인의 상태로 표현했다는 점에 있다.

일반적으로 인공지능을 적용한 제품은 모델이 확신하지 못하는 상황에서 그럴듯한 결과를 제시하거나 오류를 반환한다. 그러나 계약이라는 도메인에서 아직 결정되지 않은 상태는 예외가 아니라 정상적인 상태이다. 마찬가지로 모델이 확신하지 못하는 상황 또한 정상적인 동작에 해당한다.

본 시스템은 이 두 가지를 동일한 상태로 대응시켰다. 모델이 요구사항의 성격을 확정하지 못하면 그 결과를 미확정 상태로 기록하며, 이 상태는 그 자체로 사용자에게 판단을 요청하는 신호가 된다. 불확실성을 감추지 않는 설계가 곧 사용자의 판단 영역을 보존하는 장치로 기능하는 것이다.

이는 인공지능이 변경 사항을 관리하고 사람이 변경 여부를 판단한다는 제품 원칙이 구현 수준에서 강제되는 방식이기도 하다.

### 6.5 성능 측정

요구사항 분류의 정확도는 별도로 구축한 평가 집합을 통해 측정한다. 대화 문장과 정답 상태를 사전에 표기한 자료를 사용하며, 측정 결과와 함께 오분류 사례의 유형을 함께 제시한다. 오분류 유형을 명시하는 것은 측정이 실제로 수행되었음을 보이는 근거이며, 이후 프롬프트 개선의 기준이 된다.
