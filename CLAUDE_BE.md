# CLAUDE_BE.md

> 백엔드 작업 규약이다. `app/api/`, `core/`, `infra/`, `types/`를 수정할 때 읽는다.
> `CLAUDE.md`의 공통 규약을 먼저 읽고 이 문서를 읽는다.

---

## 1. 계층 구조

세 계층으로 나눈다. 의존 방향은 한쪽으로만 흐른다.

```
app/api/   ─────▶   core/   ◀─────   infra/
(진입점)          (도메인)          (외부 의존)
```

| 계층 | 하는 일 | 하지 않는 일 |
|---|---|---|
| `app/api/` | 요청 파싱, 응답 반환, 계층 연결 | 비즈니스 규칙 판단 |
| `core/` | 도메인 규칙, 상태 전이, 검증 | 외부 호출, 프레임워크 사용 |
| `infra/` | AI 호출, 저장소 접근, 외부 API | 도메인 규칙 판단 |

### 절대 규칙

**`core/`에는 다음이 등장하면 안 된다.**

```
next  react  fetch  process.env  window  localStorage  Anthropic
```

`core/`는 입력을 받아 출력을 내는 순수 함수의 모음이다. 이 규칙이 지켜지면 프레임워크를 바꾸거나 저장소를 교체할 때 도메인 로직을 그대로 옮길 수 있다.

외부와 통신하는 코드는 전부 `infra/`에 둔다. `app/api/`가 둘을 이어 붙인다.

---

## 2. 폴더 구성

```
app/api/                     API 진입점 (명세 확정 후 작성)

core/
├─ contract/                 계약 도메인
├─ requirement/              요구사항 도메인
│  ├─ state-machine.ts       상태 전이 규칙
│  └─ grounding.ts           근거 검증
└─ shared/
   └─ result.ts              Result 타입

infra/
├─ llm/
│  ├─ client.ts              Anthropic 호출
│  ├─ prompts/               프롬프트 (코드와 분리)
│  └─ fallback.ts            폴백 전략
└─ storage/
   └─ local.ts               localStorage 어댑터

types/index.ts               공유 타입
```

프롬프트는 코드에 문자열로 박지 않고 `infra/llm/prompts/` 아래 별도 파일로 둔다. 튜닝할 때 코드를 건드리지 않기 위해서다.

---

## 3. AI 설계 원칙

### 3.1 역할 분담

| 주체 | 담당 |
|---|---|
| **AI** | 대화에서 무엇이 있는지 찾아내고 분류한다 |
| **코드** | 승인되지 않은 것이 반영되지 않도록 막는다 |
| **사람** | 받아줄지, 얼마에, 언제까지를 결정한다 |

AI에게 상태 전이나 계약 반영 여부를 맡기지 않는다. 그것은 규칙이지 추론이 아니다.

### 3.2 문제 유형

이 시스템이 푸는 문제는 **대화 상태 추적(Dialogue State Tracking)** 이다. 대화에서 구조화된 상태를 추출해 지속적으로 갱신하는 문제다.

공개 벤치마크에서 종단간 LLM 방식은 낮은 정확도를 보이며, 추출·분류·검증을 분리한 방식이 유의미하게 높다. 아래 5계층 구조는 이 문제 유형에 대응한 설계다.

---

## 4. AI 안정성 — 5계층 검증

**전제: LLM 출력을 신뢰하지 않는다. 매 계층에서 검증하고, 실패하면 안전한 쪽으로 강등한다.**

```
대화 텍스트
   ↓
[L0] 발화 분할        speaker / text / index 로 정규화
   ↓
[L1] 스키마 강제      tool use + Zod. 실패 시 1회 재시도
   ↓
[L2] 근거 검증        인용문이 원문에 실제로 존재하는가
   ↓
[L3] 상태 전이 검증   이전 상태에서 도달 가능한가
   ↓
[L4] 사람 승인        합의 아니면 반영 불가
   ↓
상태 갱신
```

### L0. 발화 분할

대화를 통째로 넘기지 않는다. 발화 단위로 쪼개고 인덱스를 부여한다.

```ts
type Utterance = { index: number; speaker: string; text: string };
```

이 인덱스가 있어야 L2의 근거 검증과 화면의 원문 하이라이트가 가능하다.

### L1. 스키마 강제

프롬프트로 "JSON만 출력해"라고 부탁하지 않는다. tool use로 스키마를 강제하고, 받은 결과를 Zod로 한 번 더 검증한다.

검증에 실패하면 **에러 내용을 프롬프트에 덧붙여 1회만 재시도**한다. 두 번째도 실패하면 폴백으로 넘긴다. 무한 재시도는 하지 않는다.

### L2. 근거 검증

모델이 내놓은 인용문이 원문에 글자 그대로 존재하는지 확인한다. 없으면 그 항목을 버린다.

```ts
if (!utterance.text.includes(item.sourceQuote)) return null;
```

**부분 수용을 원칙으로 한다.** 5건 중 1건이 검증에 실패하면 그 1건만 버리고 나머지 4건은 살린다. 전체를 폐기하지 않는다.

### L3. 상태 전이 검증

모델이 제안한 상태가 이전 상태에서 도달 불가능하면 **거부하지 않고 안전한 상태로 강등한다.**

```ts
const next = canTransition(prev, proposed) ? proposed : '미확정';
```

거부하면 화면에 아무것도 남지 않는다. 강등하면 사람이 판단할 거리가 남는다.

### L4. 사람 승인 게이트

시스템 전체에서 계약을 변경하는 통로는 하나뿐이며, 그 통로 앞에 승인 검사가 있다.

```ts
export function applyToContract(contract, requirement) {
  if (requirement.status !== '합의') throw new Error('UNAGREED_CANNOT_APPLY');
  // ...
}
```

이 검사는 우회할 수 없어야 한다. 다른 경로로 계약을 수정하는 함수를 만들지 않는다.

---

## 5. 폴백 전략

로컬 환경에서 안정적으로 동작하는 것이 목표다. 네트워크가 끊겨도 앱이 죽지 않아야 한다.

```
1) 캐시 확인    미리 준비된 입력이면 저장된 결과를 즉시 반환
2) 실제 호출    타임아웃 8초
3) 안전 실패    빈 결과 + 사용자 안내. 예외를 화면까지 던지지 않는다
```

원칙은 세 가지다.

- **타임아웃 없는 호출을 만들지 않는다.**
- **예외를 삼키지 않되, 화면을 깨뜨리지도 않는다.** API 계층에서 잡아 안내 문구로 바꾼다.
- **재시도는 최대 1회다.** 사용자를 오래 기다리게 하지 않는다.

---

## 6. 응답 규약

모든 API는 POST와 JSON을 쓰고 다음 형태로 통일한다.

```ts
// 성공
{ ok: true, data: T }

// 실패
{ ok: false, error: string }
```

`error`는 사용자가 그대로 읽을 수 있는 한국어 문장으로 쓴다. 스택 트레이스나 내부 식별자를 노출하지 않는다.

```
"대화 내용을 분석하지 못했습니다. 다시 시도해 주세요."
```

HTTP 상태 코드는 정상 200, 잘못된 입력 400, 서버 오류 500만 쓴다. 그 외 코드는 쓰지 않는다.

구체적인 엔드포인트는 기능 명세가 확정된 뒤에 정의한다. 지금은 형태만 고정한다.

---

## 7. 타입 관리

`types/index.ts`가 공유 타입의 단일 원천이다.

- 도메인 언어로 이름을 짓는다. `Data`, `Item`, `Info` 같은 이름을 쓰지 않는다.
- 상태값은 문자열 유니온으로 쓴다. `enum`을 쓰지 않는다.
- 프론트가 함께 참조하므로, 수정하려면 먼저 알린다.

```ts
export const REQUIREMENT_STATUS = ['문의', '요청', '합의'] as const;
export type RequirementStatus = (typeof REQUIREMENT_STATUS)[number];
```

---

## 8. 백엔드 체크리스트

작업을 마치기 전에 확인한다.

- [ ] `core/`에 프레임워크 import가 없는가
- [ ] LLM 호출에 타임아웃이 있는가
- [ ] LLM 출력을 Zod로 검증했는가
- [ ] 검증 실패 시 전체를 버리지 않고 부분 수용하는가
- [ ] 계약을 변경하는 경로가 승인 검사를 지나는가
- [ ] 에러 문구가 사용자가 읽을 문장인가
- [ ] API 키가 클라이언트 코드에 노출되지 않는가
