# CLAUDE.md

> 이 저장소에서 작업하는 사람과 AI 에이전트가 공통으로 지키는 규약이다.
> 작업 시작 전에 반드시 읽는다.

---

## 0. 작업 분기 — 가장 먼저 확인할 것

**이 파일만 읽고 작업을 시작하지 않는다.** 담당 영역에 따라 아래 문서를 함께 읽는다.

| 작업 영역 | 함께 읽을 문서 |
|---|---|
| `app/page.tsx`, `app/layout.tsx`, `components/`, `app/globals.css` 수정 | **`CLAUDE_FE.md`** |
| 서버 로직 | 이 저장소가 아니다. `young-thirty/agreed_be`로 간다 |

AI 에이전트에게 작업을 시킬 때는 다음 문장을 프롬프트에 포함한다.

```
CLAUDE.md와 CLAUDE_FE.md를 먼저 읽고 규약을 지켜서 작업해.
```

> **`CLAUDE_BE.md`와 `ARCHITECTURE.md`는 이제 이 저장소의 규약이 아니다.**
> 백엔드가 별도 저장소로 분리되면서 `agreed_be`의 `CLAUDE.md`·`ARCHITECTURE.md`로
> 옮겨졌다. 기획 의도와 시연 시나리오는 `agreed_be/HANDOFF.md`에 있다.

---

## 1. 프로젝트 개요

**서비스명: Agreed**

프리랜서 개발자가 이메일·슬랙으로 들어오는 고객 메시지에 대응하는 것을 돕는 서비스다.
고객 메시지가 들어오면 계약·과거 대화·기존 티켓·GitHub 개발 상황을 대신 확인해 요구를 분석하고, 보낼 수 있는 답변 초안까지 만들어 준다.

AI가 하는 일은 딱 두 가지다.

1. **요구 분석**
2. **답변 초안 작성**

> **티켓 생성·반영, 상태 변경, 최종 요구사항 확정, 발송은 반드시 사람이 결정한다.**

금액·일정·수락 여부도 사람이 정한다. 시스템이 대신 결정하지 않는다.

---

## 2. 절대 규칙

1. **이 저장소는 프론트엔드 전용이다.** 백엔드 API는 별도 저장소(`young-thirty/agreed_be`, FastAPI)에 있다.
2. **백엔드에 붙어 있다.** 프로젝트·티켓·고객 메시지·AI 분석·자료는 전부 서버에서 받아 온다(`lib/api.ts`). 목 데이터는 없다. 사람이 내린 판단도 서버에 남고, 브라우저에는 사이드바 접힘 같은 화면 상태만 저장한다.
3. **화면의 단위는 티켓이다.** 고객 메시지가 들어오면 관련 티켓에 붙고, 관련 티켓이 없으면 `Active` 티켓이 새로 만들어진다(생성은 inbound 트리거의 몫이다).
4. **요구사항 확정과 상태 변경은 사람만 한다.** 티켓 상태는 `Active`(만들어진 순간부터 끝날 때까지) · `Done` · `Reject` 셋뿐이고, 셋 다 사람이 직접 바꾼다. 자동으로 넘어가지 않는다.
   AI는 분석하고 초안을 쓸 뿐, 요구사항을 확정하거나 상태를 바꾸지 않는다.
5. **테스트 코드를 작성하지 않는다.** 검증은 실행으로 한다.
6. **로그인이 있다.** 세션은 서버가 HttpOnly 쿠키로 내려주므로 브라우저 코드가 토큰을 들고 있지 않다. 인증 접점은 `lib/auth.ts` 하나뿐이고, 여기 없는 곳에서 인증을 다루지 않는다.
7. **`types/index.ts`는 백엔드와 공유하는 이름이다.** 바꿔야 하면 먼저 백엔드에 알린다.
8. **기능 명세가 확정된 범위 안에서 도메인 기능을 구현한다.** 확정되지 않은 범위는 임의로 확장하지 않는다.

---

## 3. 작업 원칙

AI 에이전트가 흔히 저지르는 실수를 줄이기 위한 규칙이다. 사소한 작업에는 판단해서 적용한다.

### 3.1 구현 전에 생각한다

- 전제를 말로 꺼낸다. 확신이 없으면 묻는다.
- 해석이 여러 개면 임의로 하나를 고르지 말고 전부 제시한다.
- 더 단순한 방법이 보이면 말한다. 필요하면 반대 의견을 낸다.
- 이해가 안 되면 멈춘다. 무엇이 불분명한지 이름을 붙여 묻는다.

### 3.2 단순함이 먼저다

문제를 푸는 최소한의 코드만 쓴다. 미래를 대비한 코드는 쓰지 않는다.

- 요청받지 않은 기능은 만들지 않는다.
- 한 곳에서만 쓰는 코드에 추상화 계층을 만들지 않는다.
- 요청받지 않은 확장성과 설정 가능성을 넣지 않는다.
- 일어날 수 없는 상황에 대한 예외 처리를 넣지 않는다.
- 200줄을 썼는데 50줄로 될 것 같으면 다시 쓴다.

스스로 물어본다. **"시니어 엔지니어가 이걸 보고 과하다고 할까?"** 그렇다면 줄인다.

### 3.3 최소한만 건드린다

기존 코드를 수정할 때는 필요한 곳만 만진다.

- 옆에 있는 코드, 주석, 포맷을 개선하지 않는다.
- 망가지지 않은 것을 리팩터링하지 않는다.
- 내 취향과 달라도 기존 스타일을 따른다.
- 관련 없는 죽은 코드를 발견하면 말만 하고 지우지 않는다.
- **내 변경 때문에** 쓰이지 않게 된 import와 변수는 정리한다. 원래부터 쓰이지 않던 것은 건드리지 않는다.

기준은 하나다. **바뀐 모든 줄이 요청 사항으로 직접 추적되어야 한다.**

### 3.4 검증 가능한 목표로 바꾼다

작업을 받으면 성공 조건을 먼저 정한다. 테스트 코드는 쓰지 않으므로, 실행해서 눈으로 확인할 수 있는 조건으로 정의한다.

- "검증 추가" → "잘못된 입력을 넣었을 때 화면에 안내가 뜨는가"
- "버그 수정" → "재현 절차를 밟았을 때 더 이상 발생하지 않는가"

여러 단계짜리 작업은 짧은 계획을 먼저 밝힌다.

```
1. [작업] → 확인: [확인 방법]
2. [작업] → 확인: [확인 방법]
```

성공 조건이 명확하면 혼자 반복해서 완성할 수 있다. "잘 되게 해줘" 같은 조건은 계속 되묻게 만든다.

---

## 4. 폴더 구조와 담당

```
├─ app/
│  ├─ (app)/tickets/        티켓 목록 + 티켓 상세 (핵심 화면)
│  ├─ (app)/projects/       프로젝트 목록 · 티켓 · 컨텍스트
│  ├─ layout.tsx            레이아웃
│  └─ globals.css           디자인 토큰
│
├─ components/              UI 컴포넌트 + AppStore(공유 상태)
├─ hooks/                   usePersistedState 등
├─ lib/api.ts               백엔드 호출 (화면은 여기 있는 함수만 부른다)
├─ types/index.ts           도메인 타입
└─ lib/format.ts            날짜·금액·초안 치환
```

서버 로직을 이 저장소에 두지 않는다.

### 서버와의 경계

서버는 `young-thirty/agreed_be`(FastAPI)다. 이미 붙어 있다. 지킬 것은 두 가지다.

1. **응답 규약** — 항상 `{ ok: true, data }` 또는 `{ ok: false, error }`
2. **필드 이름** — 백엔드가 camelCase로 내려주고, `types/index.ts`가 그 이름을 그대로 쓴다

호출은 `lib/api.ts`에 모여 있고 경로를 아는 곳도 거기뿐이다. 화면은 함수만 부른다.

---

## 5. 기술 스택과 인프라

| 항목 | 선택 |
|---|---|
| 프레임워크 | Next.js (App Router) |
| 언어 | TypeScript |
| 스타일 | Tailwind CSS |
| 화면 상태 | React 상태 (사이드바 접힘 등 화면 상태만 localStorage) |
| 데이터 | 백엔드 API (`lib/api.ts`) |
| 배포 | Vercel |

AI 호출과 DB는 백엔드가 담당한다. 이 저장소에는 API 키가 존재하지 않는다.

### 인프라 규칙

- 프론트 배포는 Vercel이다. 백엔드는 별도로 뜬다.
- **API 키를 이 저장소에 두지 않는다.** AI 호출은 전부 백엔드를 거친다.
- 환경변수에 `NEXT_PUBLIC_` 접두사를 붙이면 브라우저에 노출된다. 백엔드 주소처럼 노출돼도 되는 값에만 쓴다.
- 프론트를 배포하고 백엔드를 로컬에 두면 https → http라 브라우저가 차단한다. **둘 다 로컬이거나 둘 다 배포다.**
- 새 라이브러리를 추가하기 전에 팀에 묻는다.

---

## 6. Git 컨벤션

### 브랜치

- **`dev`가 기본 브랜치다.**
- **지금(해커톤 스프린트) 기본 흐름은 `dev`에 직접 커밋이다.** 이슈·브랜치 절차는 생략한다. 격리가 필요한 큰 변경이면 이전처럼 `타입/#이슈번호` 브랜치를 파도 된다 — 그때도 결국 `dev`로 병합한다.

### 작업 순서 (dev 직접 커밋)

```
1. dev로 이동하고 원격을 최신화한다   → git switch dev && git fetch origin dev && git merge --ff-only origin/dev
2. 작업하고 커밋한다                  → feat: 요구사항 카드 컴포넌트 추가
3. 푸시 직전 다시 원격을 확인한다      → git fetch origin dev && git merge origin/dev
4. 푸시한다                          → git push origin dev
```

**3번을 건너뛰지 않는다.** 여러 명이 같은 브랜치에 동시에 커밋하므로, 푸시 전에 원격이 앞서 있으면 먼저 받아 합친 뒤(충돌 있으면 해결) 푸시한다. `--force`는 쓰지 않는다.

### 양식

이슈·커밋·푸시 모두 같은 형식을 쓴다.

```
타입: 작업내용요약
```

예시:

```
feat: 요구사항 카드 컴포넌트 추가
fix: 상태 전이 검증 누락 수정
chore: 프로젝트 초기 세팅
docs: 규약 문서 추가
```

### 타입

| 타입 | 용도 |
|---|---|
| `feat` | 기능 추가 |
| `fix` | 버그 수정 |
| `chore` | 설정, 의존성, 잡무 |
| `docs` | 문서 |
| `style` | 포맷, 스타일 |

이슈 제목, 커밋 메시지, PR 제목에 모두 같은 형식을 쓴다. 브랜치 이름만 `타입/#번호` 형식이다.

### 커밋 습관

- 30분에 한 번은 커밋한다.
- 한 커밋에 한 가지 작업만 담는다.
- 요약은 한국어로 쓴다.
- 브랜치를 오래 살려두지 않는다. 하루 안에 병합한다.

---

## 7. 하지 않는 것

시간과 복잡도를 낭비하는 목록이다. 요청받아도 되묻는다.

- 테스트 코드
- `lib/auth.ts` 밖에서 인증을 다루는 것 (로그인·회원가입은 이미 있다)
- 프론트에서의 직접 AI 호출 (백엔드를 거친다)
- 전역 상태 라이브러리 (Redux, Zustand, Recoil)
- 모바일 반응형
- 다크 모드
- 국제화
- 과도한 로딩 상태와 에러 바운더리
- 요청받지 않은 리팩터링

---

## 8. AI 에이전트에게

- 주석과 커밋 메시지는 한국어로 쓴다.
- 기존 파일을 통째로 다시 쓰지 않는다. 필요한 부분만 고친다.
- 새 의존성을 추가하기 전에 묻는다.
- `types/index.ts`를 수정하는 변경은 먼저 알린다.
- 에러 메시지는 사용자가 읽을 문장으로 쓴다. 무엇이 잘못됐고 어떻게 하면 되는지를 담는다.
- 확신이 없으면 코드를 쓰기 전에 묻는다.
