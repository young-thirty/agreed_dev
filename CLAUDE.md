# CLAUDE.md

> 이 저장소에서 작업하는 사람과 AI 에이전트가 공통으로 지키는 규약이다.
> 작업 시작 전에 반드시 읽는다.

---

## 0. 작업 분기 — 가장 먼저 확인할 것

**이 파일만 읽고 작업을 시작하지 않는다.** 담당 영역에 따라 아래 문서를 함께 읽는다.

| 작업 영역 | 함께 읽을 문서 |
|---|---|
| `app/api/`, `core/`, `infra/`, `types/` 수정 | **`CLAUDE_BE.md`** |
| `app/page.tsx`, `app/layout.tsx`, `components/`, `app/globals.css` 수정 | **`CLAUDE_FE.md`** |
| 양쪽에 걸치는 작업 | 둘 다 |

AI 에이전트에게 작업을 시킬 때는 다음 문장을 프롬프트에 포함한다.

```
CLAUDE.md와 CLAUDE_BE.md를 먼저 읽고 규약을 지켜서 작업해.
```

---

## 1. 프로젝트 개요

**서비스명: Agreed**

계약이 체결된 이후 고객과 오가는 대화에서 새로 발생한 요구사항과 합의의 변화를 추적해, 계약 범위·일정·비용을 항상 최신 상태로 유지하는 서비스다.

핵심 원칙은 하나다.

> **AI는 무엇이 바뀌었는지 관리하고, 사람은 바꿔도 되는지 판단한다.**

금액·일정·수락 여부는 사람이 결정한다. 시스템이 대신 결정하지 않는다.

---

## 2. 절대 규칙

1. **단일 레포다.** 프론트엔드와 백엔드가 한 Next.js 프로젝트 안에 있다. 별도 서버를 만들지 않는다.
2. **DB를 쓰지 않는다.** 상태는 React 상태와 localStorage로 관리한다.
3. **테스트 코드를 작성하지 않는다.** 검증은 실행으로 한다.
4. **인증·로그인이 없다.** 사용자 개념 자체가 없다.
5. **`types/index.ts`가 공유 타입의 단일 원천이다.** 수정하려면 먼저 팀에 알린다.
6. **기능 명세가 확정된 범위 안에서 도메인 기능을 구현한다.** 확정되지 않은 범위는 임의로 확장하지 않는다.

---

## 3. 작업 원칙

AI 에이전트가 흔히 저지르는 실수를 줄이기 위한 규칙이다. 사소한 작업에는 판단해서 적용한다.

### 3.1 구현 전에 생각한다

- 전제를 말로 꺼낸다. 확신이 없으면 묻는다.
- 해석이 여러 개면 임의로 하나를 고르지 말고 전부 제시한다.
- 더 단순한 방법이 보이면 말한다. 필요하면 반대 의견을 낸다.
- 이해가 안 되면 멈춘다. 무엇이 불분명한지 이름을 붙여 묻는다.

### 3.2 단순함이 먼저다

문제를 푸는 최소한의 코드만 쓴다. 미래를 대비한 코드는 쓰지 않는다.

- 요청받지 않은 기능은 만들지 않는다.
- 한 곳에서만 쓰는 코드에 추상화 계층을 만들지 않는다.
- 요청받지 않은 확장성과 설정 가능성을 넣지 않는다.
- 일어날 수 없는 상황에 대한 예외 처리를 넣지 않는다.
- 200줄을 썼는데 50줄로 될 것 같으면 다시 쓴다.

스스로 물어본다. **"시니어 엔지니어가 이걸 보고 과하다고 할까?"** 그렇다면 줄인다.

### 3.3 최소한만 건드린다

기존 코드를 수정할 때는 필요한 곳만 만진다.

- 옆에 있는 코드, 주석, 포맷을 개선하지 않는다.
- 망가지지 않은 것을 리팩터링하지 않는다.
- 내 취향과 달라도 기존 스타일을 따른다.
- 관련 없는 죽은 코드를 발견하면 말만 하고 지우지 않는다.
- **내 변경 때문에** 쓰이지 않게 된 import와 변수는 정리한다. 원래부터 쓰이지 않던 것은 건드리지 않는다.

기준은 하나다. **바뀐 모든 줄이 요청 사항으로 직접 추적되어야 한다.**

### 3.4 검증 가능한 목표로 바꾼다

작업을 받으면 성공 조건을 먼저 정한다. 테스트 코드는 쓰지 않으므로, 실행해서 눈으로 확인할 수 있는 조건으로 정의한다.

- "검증 추가" → "잘못된 입력을 넣었을 때 화면에 안내가 뜨는가"
- "버그 수정" → "재현 절차를 밟았을 때 더 이상 발생하지 않는가"

여러 단계짜리 작업은 짧은 계획을 먼저 밝힌다.

```
1. [작업] → 확인: [확인 방법]
2. [작업] → 확인: [확인 방법]
```

성공 조건이 명확하면 혼자 반복해서 완성할 수 있다. "잘 되게 해줘" 같은 조건은 계속 되묻게 만든다.

---

## 4. 폴더 구조와 담당

```
├─ app/
│  ├─ page.tsx              화면              → 프론트
│  ├─ layout.tsx            레이아웃           → 프론트
│  ├─ globals.css           디자인 토큰        → 프론트
│  └─ api/                  서버 로직          → 백엔드
│
├─ core/                    도메인 계층        → 백엔드
│  └─ (프레임워크 무관 순수 TypeScript)
│
├─ infra/                   외부 의존 계층     → 백엔드
│  ├─ llm/                  AI 호출
│  └─ storage/              저장소 어댑터
│
├─ components/              UI 컴포넌트        → 프론트
├─ types/index.ts           공유 타입          → 백엔드가 확정, 양쪽이 참조
└─ lib/                     공용 유틸          → 수정 시 공지
```

### 담당 경계

- 프론트는 `app/api/`, `core/`, `infra/`를 수정하지 않는다.
- 백엔드는 `components/`, `app/page.tsx`를 수정하지 않는다.
- `types/index.ts`와 `lib/`는 공용이다. 바꾸기 전에 알린다.

### 계층 규칙

**`core/`는 프레임워크를 import하지 않는다.** `next`, `react`, `fetch`가 등장하면 안 된다. 순수 함수와 타입만 둔다. 이 규칙이 지켜져야 나중에 저장소나 프레임워크를 바꿀 때 도메인 로직을 그대로 옮길 수 있다.

외부에 의존하는 코드는 전부 `infra/`에 둔다.

---

## 5. 기술 스택과 인프라

| 항목 | 선택 |
|---|---|
| 프레임워크 | Next.js (App Router) |
| 언어 | TypeScript |
| 스타일 | Tailwind CSS |
| 검증 | Zod |
| AI | Anthropic API |
| 저장소 | localStorage (DB 없음) |
| 배포 | Vercel |

### 인프라 규칙

- 배포는 Vercel 한 곳이다. 별도 서버나 컨테이너를 만들지 않는다.
- 비밀 키는 `.env.local`에만 둔다. 클라이언트 코드에서 참조하지 않는다.
- 환경변수에 `NEXT_PUBLIC_` 접두사를 붙이면 브라우저에 노출된다. API 키에는 절대 붙이지 않는다.
- 새 라이브러리를 추가하기 전에 팀에 묻는다.

---

## 6. Git 컨벤션

### 브랜치

- **`dev`가 기본 브랜치다.**
- **지금(해커톤 스프린트) 기본 흐름은 `dev`에 직접 커밋이다.** 이슈·브랜치 절차는 생략한다. 격리가 필요한 큰 변경이면 이전처럼 `타입/#이슈번호` 브랜치를 파도 된다 — 그때도 결국 `dev`로 병합한다.

### 작업 순서 (dev 직접 커밋)

```
1. dev로 이동하고 원격을 최신화한다   → git switch dev && git fetch origin dev && git merge --ff-only origin/dev
2. 작업하고 커밋한다                  → feat: 요구사항 카드 컴포넌트 추가
3. 푸시 직전 다시 원격을 확인한다      → git fetch origin dev && git merge origin/dev
4. 푸시한다                          → git push origin dev
```

**3번을 건너뛰지 않는다.** 여러 명이 같은 브랜치에 동시에 커밋하므로, 푸시 전에 원격이 앞서 있으면 먼저 받아 합친 뒤(충돌 있으면 해결) 푸시한다. `--force`는 쓰지 않는다.

### 양식

이슈·커밋·푸시 모두 같은 형식을 쓴다.

```
타입: 작업내용요약
```

예시:

```
feat: 요구사항 카드 컴포넌트 추가
fix: 상태 전이 검증 누락 수정
chore: 프로젝트 초기 세팅
docs: 규약 문서 추가
```

### 타입

| 타입 | 용도 |
|---|---|
| `feat` | 기능 추가 |
| `fix` | 버그 수정 |
| `chore` | 설정, 의존성, 잡무 |
| `docs` | 문서 |
| `style` | 포맷, 스타일 |

이슈 제목, 커밋 메시지, PR 제목에 모두 같은 형식을 쓴다. 브랜치 이름만 `타입/#번호` 형식이다.

### 커밋 습관

- 30분에 한 번은 커밋한다.
- 한 커밋에 한 가지 작업만 담는다.
- 요약은 한국어로 쓴다.
- 브랜치를 오래 살려두지 않는다. 하루 안에 병합한다.

---

## 7. 하지 않는 것

시간과 복잡도를 낭비하는 목록이다. 요청받아도 되묻는다.

- 테스트 코드
- 로그인, 인증, 사용자 관리
- 데이터베이스, ORM, 마이그레이션
- 별도 백엔드 서버
- 모바일 반응형
- 다크 모드
- 국제화
- 과도한 로딩 상태와 에러 바운더리
- 요청받지 않은 리팩터링

---

## 8. AI 에이전트에게

- 주석과 커밋 메시지는 한국어로 쓴다.
- 기존 파일을 통째로 다시 쓰지 않는다. 필요한 부분만 고친다.
- 새 의존성을 추가하기 전에 묻는다.
- `types/index.ts`를 수정하는 변경은 먼저 알린다.
- 에러 메시지는 사용자가 읽을 문장으로 쓴다. 무엇이 잘못됐고 어떻게 하면 되는지를 담는다.
- 확신이 없으면 코드를 쓰기 전에 묻는다.
