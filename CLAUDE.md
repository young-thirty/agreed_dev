# CLAUDE.md

> 이 저장소에서 작업하는 사람과 AI 에이전트가 공통으로 지키는 규약이다.
> 작업 시작 전에 반드시 읽는다.

---

## 0. 작업 분기 — 가장 먼저 확인할 것

**이 파일만 읽고 작업을 시작하지 않는다.** 담당 영역에 따라 아래 문서를 함께 읽는다.

| 작업 영역 | 함께 읽을 문서 |
|---|---|
| `app/`, `components/`, `hooks/`, `lib/api-client.ts`, `types/` 수정 | **`CLAUDE_FE.md`** |
| 서버/API 로직 | 이 저장소가 아니다. `young-thirty/agreed_be`의 규약을 읽는다 |

AI 에이전트에게 작업을 시킬 때는 다음 문장을 프롬프트에 포함한다.

```
CLAUDE.md와 CLAUDE_FE.md를 먼저 읽고 규약을 지켜서 작업해.
```

---

## 1. 프로젝트 개요

**서비스명: Agreed**

계약이 체결된 이후 고객과 오가는 대화에서 새로 발생한 요구사항과 합의의 변화를 추적해, 계약 범위·일정·비용을 항상 최신 상태로 유지하는 서비스다.

핵심 원칙은 하나다.

> **AI는 무엇이 바뀌었는지 관리하고, 사람은 바꿔도 되는지 판단한다.**

금액·일정·수락 여부는 사람이 결정한다. 시스템이 대신 결정하지 않는다.

---

## 2. 절대 규칙

1. **이 저장소는 프론트엔드 전용이다.** API, MongoDB, AI, Gmail·Slack OAuth는 별도 FastAPI 저장소(`young-thirty/agreed_be`)가 담당한다.
2. **이메일·비밀번호 로그인이 있다.** Google·Slack 연결은 로그인과 별개의 외부 채널 연동이다.
3. **서버 데이터의 원천은 MongoDB다.** 사용자, 프로젝트, 계약, 문서, 대화, AI 결과를 localStorage에 저장하지 않는다.
4. **인증은 서버의 HttpOnly 세션 쿠키를 사용한다.** JWT나 provider token을 JavaScript·localStorage에서 다루지 않는다.
5. **`types/index.ts`는 백엔드 응답 스키마를 따라간다.** 수정하려면 먼저 백엔드와 맞춘다.
6. **기능 명세가 확정되기 전에는 도메인 기능을 구현하지 않는다.** 지금은 뼈대만 세운다.

---

## 3. 작업 원칙

AI 에이전트가 흔히 저지르는 실수를 줄이기 위한 규칙이다. 사소한 작업에는 판단해서 적용한다.

### 3.1 구현 전에 생각한다

- 전제를 말로 꺼낸다. 확신이 없으면 묻는다.
- 해석이 여러 개면 임의로 하나를 고르지 말고 전부 제시한다.
- 더 단순한 방법이 보이면 말한다. 필요하면 반대 의견을 낸다.
- 이해가 안 되면 멈춘다. 무엇이 불분명한지 이름을 붙여 묻는다.

### 3.2 단순함이 먼저다

문제를 푸는 최소한의 코드만 쓴다. 미래를 대비한 코드는 쓰지 않는다.

- 요청받지 않은 기능은 만들지 않는다.
- 한 곳에서만 쓰는 코드에 추상화 계층을 만들지 않는다.
- 요청받지 않은 확장성과 설정 가능성을 넣지 않는다.
- 일어날 수 없는 상황에 대한 예외 처리를 넣지 않는다.
- 200줄을 썼는데 50줄로 될 것 같으면 다시 쓴다.

스스로 물어본다. **"시니어 엔지니어가 이걸 보고 과하다고 할까?"** 그렇다면 줄인다.

### 3.3 최소한만 건드린다

기존 코드를 수정할 때는 필요한 곳만 만진다.

- 옆에 있는 코드, 주석, 포맷을 개선하지 않는다.
- 망가지지 않은 것을 리팩터링하지 않는다.
- 내 취향과 달라도 기존 스타일을 따른다.
- 관련 없는 죽은 코드를 발견하면 말만 하고 지우지 않는다.
- **내 변경 때문에** 쓰이지 않게 된 import와 변수는 정리한다. 원래부터 쓰이지 않던 것은 건드리지 않는다.

기준은 하나다. **바뀐 모든 줄이 요청 사항으로 직접 추적되어야 한다.**

### 3.4 검증 가능한 목표로 바꾼다

작업을 받으면 성공 조건을 먼저 정한다. 테스트 코드는 쓰지 않으므로, 실행해서 눈으로 확인할 수 있는 조건으로 정의한다.

- "검증 추가" → "잘못된 입력을 넣었을 때 화면에 안내가 뜨는가"
- "버그 수정" → "재현 절차를 밟았을 때 더 이상 발생하지 않는가"

여러 단계짜리 작업은 짧은 계획을 먼저 밝힌다.

```
1. [작업] → 확인: [확인 방법]
2. [작업] → 확인: [확인 방법]
```

성공 조건이 명확하면 혼자 반복해서 완성할 수 있다. "잘 되게 해줘" 같은 조건은 계속 되묻게 만든다.

---

## 4. 폴더 구조와 담당

```
├─ app/                     Next.js 화면·레이아웃
├─ components/              UI 컴포넌트
├─ hooks/                   화면 전용 훅
├─ types/index.ts           백엔드 응답 타입을 반영
└─ lib/api-client.ts        FastAPI 호출 래퍼
```

### 담당 경계

- 이 저장소에 `app/api/`, `core/`, `infra/` 서버 코드를 새로 만들지 않는다.
- `feat/#6`의 Gmail·Slack 서버 코드는 동작 확인용 원본이며 FastAPI로 이관한다. 병합 후에는 화면 컴포넌트와 API 호출부만 남긴다.
- `types/index.ts`는 FastAPI의 camelCase 응답을 그대로 반영한다.

### 계층 규칙

API 호출은 `lib/api-client.ts` 한 곳을 통하며 FastAPI 주소를 기준으로 한다. 쿠키 세션을 보내도록 `credentials: 'include'`를 항상 사용한다.

---

## 5. 기술 스택과 인프라

| 항목 | 선택 |
|---|---|
| 프레임워크 | Next.js (App Router) |
| 언어 | TypeScript |
| 스타일 | Tailwind CSS |
| 서버 | FastAPI (`agreed_be`) |
| 인증 | 서버측 opaque session + HttpOnly cookie |
| 저장소 | MongoDB (`agreed_be`가 관리) |
| AI | DeepSeek (`agreed_be`가 호출) |
| 배포 | 프론트·백엔드 모두 AWS 기준 |

### 인프라 규칙

- 이 저장소에는 AI·Google·Slack client secret과 provider token을 두지 않는다.
- 프론트 환경변수에는 브라우저에 공개되어도 되는 FastAPI 주소만 둔다.
- 환경변수에 `NEXT_PUBLIC_` 접두사를 붙이면 브라우저에 노출된다. API 키에는 절대 붙이지 않는다.
- 새 라이브러리를 추가하기 전에 팀에 묻는다.

---

## 6. Git 컨벤션

### 브랜치

- **`dev`가 기본 브랜치다.** 모든 작업 브랜치는 `dev`에서 따고 `dev`로 병합한다.
- **`dev`에 직접 커밋하지 않는다.** 작업마다 브랜치를 판다.

브랜치 이름은 이슈 번호를 붙여 다음 형식으로 만든다.

```
타입/#이슈번호
```

예시:

```
feat/#12
fix/#15
chore/#3
```

### 작업 순서

```
1. 이슈를 먼저 만든다          → feat: 요구사항 카드 컴포넌트 추가
2. dev에서 최신 코드를 받는다   → git switch dev && git pull
3. 브랜치를 판다               → git switch -c feat/#12
4. 작업하고 커밋한다            → feat: 요구사항 카드 컴포넌트 추가
5. 푸시한다                    → git push -u origin feat/#12
6. dev로 PR을 올린다           → feat: 요구사항 카드 컴포넌트 추가
7. 병합 후 브랜치를 지운다
```

이슈를 먼저 만들어야 브랜치 이름을 정할 수 있다. 순서를 바꾸지 않는다.

### 양식

이슈·커밋·푸시 모두 같은 형식을 쓴다.

```
타입: 작업내용요약
```

예시:

```
feat: 요구사항 카드 컴포넌트 추가
fix: 상태 전이 검증 누락 수정
chore: 프로젝트 초기 세팅
docs: 규약 문서 추가
```

### 타입

| 타입 | 용도 |
|---|---|
| `feat` | 기능 추가 |
| `fix` | 버그 수정 |
| `chore` | 설정, 의존성, 잡무 |
| `docs` | 문서 |
| `style` | 포맷, 스타일 |

이슈 제목, 커밋 메시지, PR 제목에 모두 같은 형식을 쓴다. 브랜치 이름만 `타입/#번호` 형식이다.

### 커밋 습관

- 30분에 한 번은 커밋한다.
- 한 커밋에 한 가지 작업만 담는다.
- 요약은 한국어로 쓴다.
- 브랜치를 오래 살려두지 않는다. 하루 안에 병합한다.

---

## 7. 하지 않는 것

시간과 복잡도를 낭비하는 목록이다. 요청받아도 되묻는다.

- Next.js API route에서 Gmail·Slack OAuth 또는 AI 호출
- 사용자·프로젝트·계약·문서·대화·AI 결과를 localStorage에 영속화
- access token, refresh token, Slack bot token을 브라우저 쿠키 값이나 localStorage로 직접 관리
- 로그인과 Google·Slack 연동을 하나의 흐름으로 취급
- 모바일 반응형
- 다크 모드
- 국제화
- 과도한 로딩 상태와 에러 바운더리
- 요청받지 않은 리팩터링

---

## 8. AI 에이전트에게

- 주석과 커밋 메시지는 한국어로 쓴다.
- 기존 파일을 통째로 다시 쓰지 않는다. 필요한 부분만 고친다.
- 새 의존성을 추가하기 전에 묻는다.
- `types/index.ts`를 수정하는 변경은 먼저 알린다.
- 에러 메시지는 사용자가 읽을 문장으로 쓴다. 무엇이 잘못됐고 어떻게 하면 되는지를 담는다.
- 확신이 없으면 코드를 쓰기 전에 묻는다.
