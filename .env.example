# 이 파일을 .env.local로 복사한다.
#   cp .env.example .env.local
#
# 프론트에는 공개되어도 되는 백엔드 주소만 둔다.
# DeepSeek·Google·Slack secret은 agreed_be/.env 또는 AWS Secrets Manager에 둔다.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
