# 이 파일을 .env.local로 복사한다.
#   cp .env.example .env.local
#
# 프론트에는 공개되어도 되는 FastAPI 주소만 둔다.
# DeepSeek·Google·Slack secret과 redirect URI는 agreed_be가 관리한다.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
