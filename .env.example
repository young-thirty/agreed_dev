# 이 파일을 .env.local로 복사한 뒤 값을 채운다.
#   cp .env.example .env.local
#
# 이 저장소는 프론트엔드 전용이다. API 키·시크릿을 여기에 두지 않는다 — 전부 agreed_be가 관리한다.
# NEXT_PUBLIC_ 접두사를 붙이면 브라우저에 그대로 노출된다. 공개돼도 되는 값에만 쓴다.

# Gmail·Slack 연동을 처리하는 FastAPI 백엔드(agreed_be) 주소.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
