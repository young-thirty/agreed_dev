# CLAUDE_FE.md

> 프론트엔드 작업 규약이다. `app/`, `components/`, `hooks/`, `lib/api-client.ts`, `types/`를 수정할 때 읽는다.
> `CLAUDE.md`의 공통 규약을 먼저 읽고 이 문서를 읽는다.

---

## 1. 담당 범위

```
app/page.tsx          메인 화면. 상태를 여기서 들고 내려준다
app/layout.tsx        전역 레이아웃
app/globals.css       디자인 토큰
components/           재사용 컴포넌트
hooks/                화면 편의 상태 훅
lib/api-client.ts     FastAPI 호출 래퍼
types/                백엔드 공개 응답 타입
```

`app/api/`, `core/`, `infra/`는 건드리지 않는다. 필요한 변경이 있으면 백엔드에 요청한다.

---

## 2. 상태 관리

### 전역 상태 라이브러리를 쓰지 않는다

Redux, Zustand, Recoil을 도입하지 않는다. `app/page.tsx`가 상태를 소유하고 props로 내려준다. 화면이 하나뿐이라 이걸로 충분하다.

### 서버 데이터를 localStorage에 저장하지 않는다

사용자·계약·문서·Gmail·Slack 대화·AI 결과는 새로고침할 때 FastAPI에서 다시 받는다.
localStorage는 선택한 탭이나 Slack 워크스페이스·채널처럼 유출돼도 무해한 UI 설정에만 쓴다.
로그인 세션과 provider token은 JavaScript에서 읽거나 저장하지 않는다.

### 초기화 버튼은 필수다

화면 편의값 초기화와 서버 데이터 초기화는 구분한다. 서버 데이터를 지우는 기능이 필요하면 백엔드 API로 별도 구현한다.

---

## 3. 서버 호출

`fetch`를 직접 부르지 않는다. `lib/api-client.ts`의 래퍼를 쓴다.

래퍼는 `NEXT_PUBLIC_API_BASE_URL`을 기준으로 호출하고 모든 요청에
`credentials: 'include'`를 넣어 HttpOnly 세션 쿠키를 보낸다.

```ts
const res = await post<Requirement[]>('/api/...', payload);
if (!res.ok) {
  // res.error를 화면에 표시한다
  return;
}
// res.data 사용
```

응답 형태는 항상 `{ ok, data }` 또는 `{ ok, error }` 다. 이 규약은 백엔드와 공유한다.

### 백엔드를 기다리지 않는다

API가 준비되기 전에는 목 데이터로 화면을 완성한다. 타입이 같으므로 나중에 호출로 교체할 때 컴파일 에러 없이 바뀐다.

---

## 4. 컴포넌트 규칙

- 한 파일에 한 컴포넌트를 둔다.
- props에 타입을 명시한다. `any`를 쓰지 않는다.
- 공유 타입은 `@/types`에서 가져온다. 컴포넌트 파일에 도메인 타입을 새로 정의하지 않는다.
- 상태를 컴포넌트 안에 숨기지 않는다. 화면 전체가 알아야 할 상태는 `page.tsx`에 둔다.
- 컴포넌트가 200줄을 넘으면 쪼갠다.

### 라이브러리 추가

새 UI 라이브러리를 도입하기 전에 팀에 묻는다. 컴포넌트 라이브러리를 통째로 들여오면 스타일 충돌과 번들 문제가 생긴다.

---

## 5. 스타일

### Tailwind만 쓴다

CSS 모듈, styled-components, emotion을 쓰지 않는다.

### 색은 변수로만

`app/globals.css`에 정의된 CSS 변수를 통해서만 색을 쓴다. 컴포넌트에 hex 값을 직접 적지 않는다.

```tsx
// 좋음
<div className="text-[--color-ink]" />

// 나쁨
<div style={{ color: '#1a1a18' }} />
```

팔레트를 바꾸고 싶으면 `globals.css` 한 곳만 고친다.

### 반응형

데스크톱만 맞춘다. 시연은 노트북 화면에서 한다. 모바일 대응에 시간을 쓰지 않는다.

다만 **노트북 해상도에서 잘리지 않는지는 반드시 확인한다.** 1440×900 정도에서 스크롤 없이 핵심 화면이 보여야 한다.

---

## 6. 접근성 기본선

과하게 하지 않되 아래는 지킨다.

- 클릭 가능한 요소는 `<button>`을 쓴다. `<div onClick>`을 쓰지 않는다.
- 포커스 표시를 지우지 않는다. `outline: none`을 쓰지 않는다.
- 이미지에 `alt`를 넣는다.
- 모션 최소화 설정을 존중한다. `globals.css`에 이미 반영돼 있다.

---

## 7. 문구 작성

화면에 들어가는 글도 설계의 일부다.

- 사용자가 아는 말로 쓴다. 시스템 내부 용어를 노출하지 않는다.
- 버튼은 눌렀을 때 일어나는 일을 그대로 적는다. "확인"보다 "변경 반영"이 낫다.
- 같은 동작은 화면 전체에서 같은 이름을 쓴다.
- 에러는 무엇이 잘못됐고 어떻게 하면 되는지를 담는다. 사과하지 않고, 모호하게 쓰지 않는다.
- 빈 화면은 다음에 무엇을 하면 되는지 안내한다.

```
나쁨:  "오류가 발생했습니다."
좋음:  "대화 내용을 분석하지 못했습니다. 다시 시도해 주세요."
```

---

## 8. 시연 안정성

현장에서 직접 시연하므로 화면이 죽지 않는 것이 최우선이다.

- 로딩 중 상태를 반드시 표시한다. 아무 반응이 없으면 시연자가 두 번 누르게 된다.
- 서버 응답이 실패해도 화면이 흰 화면으로 가지 않는다. 안내 문구를 보여주고 이전 상태를 유지한다.
- 배열이 비었을 때, 값이 `null`일 때를 처리한다. 시연 중 가장 흔한 사고다.
- 콘솔에 에러가 남지 않게 한다. 심사위원이 개발자 도구를 열어볼 수 있다.

---

## 9. 프론트 체크리스트

작업을 마치기 전에 확인한다.

- [ ] 새로고침 후 서버 데이터를 다시 불러오는가
- [ ] 서버 데이터나 인증·연동 토큰을 localStorage에 저장하지 않았는가
- [ ] 서버 응답이 실패해도 화면이 유지되는가
- [ ] 빈 배열, `null` 상태에서 깨지지 않는가
- [ ] 노트북 해상도에서 잘리지 않는가
- [ ] 콘솔에 경고와 에러가 없는가
- [ ] 색을 hex로 직접 적은 곳이 없는가
